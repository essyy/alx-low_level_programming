#include <stdlib.h>
#include <time.h>
/* main - Entry point
 *
 *Return: Success is 0 (Always)
 *
 */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if ()
	return (0);
}
