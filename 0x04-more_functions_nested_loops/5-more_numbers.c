#include "main.h"

/**
 *  more_numbers -  function that prints 10 times the numbers
 *
 */
void more_numbers(void)
{
	int c, i;

	for (c = 1; c <= 10; c++)
	{
		for (i = 0; i <= 14; i++)
		{
			_putchar(i);
		}
	 _putchar('\n');
	}
}
